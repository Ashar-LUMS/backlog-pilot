{
  "name": "backlog-pilot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm run build --workspace client",
    "build": "npm run build --workspace client"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
